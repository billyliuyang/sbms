<p id="notice"><%= notice %></p>

<h1>Emotions</h1>

<table>
  <thead>
    <tr>
      <th>Current hr</th>
      <th>Emotion v</th>
      <th>Emotion a</th>
      <th>User</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @emotions.each do |emotion| %>
      <tr>
        <td><%= emotion.current_hr %></td>
        <td><%= emotion.emotion_v %></td>
        <td><%= emotion.emotion_a %></td>
        <td><%= emotion.user %></td>
        <td><%= link_to 'Show', emotion %></td>
        <td><%= link_to 'Edit', edit_emotion_path(emotion) %></td>
        <td><%= link_to 'Destroy', emotion, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Emotion', new_emotion_path %>
