<%= form_for(emotion) do |f| %>
  <% if emotion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(emotion.errors.count, "error") %> prohibited this emotion from being saved:</h2>

      <ul>
      <% emotion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :current_hr %>
    <%= f.number_field :current_hr %>
  </div>

  <div class="field">
    <%= f.label :emotion_v %>
    <%= f.number_field :emotion_v %>
  </div>

  <div class="field">
    <%= f.label :emotion_a %>
    <%= f.number_field :emotion_a %>
  </div>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
